{
  "name": "site-01-moscow-avtokran",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "images": "node scripts/to-webp.mjs",
    "postbuild": "node scripts/generate-redirects.mjs && node scripts/generate-sitemap.mjs",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "sharp": "^0.35.3",
    "wrangler": "^4.123.0"
  }
}
