/**
 * Одна дата на весь прайс сайта.
 *
 * До этой волны срок действия цен существовал ТОЛЬКО в разметке (priceValidUntil
 * в тринадцати Offer'ах парка) и нигде не был виден человеку — а посетитель,
 * который читает цену, и парсер, который её индексирует, обязаны видеть одно и
 * то же. Теперь значение одно и берётся отсюда: компонент PriceDisclaimer
 * печатает его рядом с ценой в <time datetime>, а Offer'ы отдают его же в
 * priceValidUntil.
 *
 * При пересмотре ставок правится ЗДЕСЬ и в priceValidUntil страниц парка —
 * оба места обязаны сойтись, иначе видимый текст и разметка разойдутся.
 */
export const PRICES_VALID_UNTIL = '2027-02-17';
