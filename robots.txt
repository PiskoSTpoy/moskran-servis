# Сайт открыт целиком: закрытых разделов, черновиков и служебных копий нет,
# поэтому запрещать нечего.
User-agent: *
Allow: /

# Ниже — те же разрешения, но выписанные на каждого ассистента отдельной
# строкой. Сегодня они ничего не меняют. Нужны они на будущее: как только в
# общий блок попадёт хоть один Disallow, ассистенты замолчат первыми, и
# понять это по позициям в обычном поиске будет нельзя — там ничего не
# просядет. Именной блок сильнее «*», поэтому переживает такую правку.

# Яндекс: показ в нейроответах и у Алисы. Имени два, работают в паре —
# оставить одно и забыть второе значит открыть половину канала.
User-agent: YandexAdditional
Allow: /

User-agent: YandexAdditionalBot
Allow: /

# Perplexity.
User-agent: PerplexityBot
Allow: /

# ChatGPT: обход ради ответа со ссылкой. Это не тот робот, что собирает
# обучающую выборку, — про сбор данных здесь не сказано ничего.
User-agent: OAI-SearchBot
Allow: /

# Claude: поисковая часть, отдельная от обучающей.
User-agent: Claude-SearchBot
Allow: /

Sitemap: https://moskran-servis.ru/sitemap.xml
